name: Sende Instagram-Grafiken an Telegram

on:
  workflow_dispatch:  # Manuelles Auslösen
  schedule:
    - cron: '30 5 * * *'  # Täglich um 9:30 Uhr Dubai-Zeit (5:30 UTC)

jobs:
  send-graphics:
    runs-on: ubuntu-latest

    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

    steps:
      - name: 📥 Repository auschecken
        uses: actions/checkout@v3

      - name: 🖼️ Sende Grafiken an Telegram
        run: |
          for file in graphics/*.png; do
            echo "📨 Sende $file an Telegram..."
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendPhoto" \
              -F chat_id="${TELEGRAM_CHAT_ID}" \
              -F photo=@"$file" \
              || echo "❌ Fehler beim Senden von $file"
          done
